
export type Company = {
    id: number
    name: string
    phone: string
    address: string
    industry: number
}

export type Worker = {
    id: number
    full_name: string
    profession: string
}